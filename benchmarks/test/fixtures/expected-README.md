# Benchmarks

<!-- Generated by benchmarks/report.mjs. Do not edit by hand -- run
     `node benchmarks/report.mjs` (or `--check` to verify it is current).
     The Methodology and Limits prose lives in benchmarks/METHODOLOGY.md. -->

3 recorded runs across 2 platforms (`linux-x86_64`, `macos-arm64`).
Newest: **slim-v26.6.0-2** on `macos-arm64`, measured 2026-07-15T10:30:00.000Z.

> **Note:** 1 result file uses a schema version this generator does not know (expected `schemaVersion: 1`). Rendered best-effort; fields it cannot map show as `—`. Affected: `history/macos-arm64/slim-v26.6.0-2.json`.

## Latest run

### `linux-x86_64` — slim-v26.6.0-2

| field | value |
| --- | --- |
| commit | `2222222222222222222222222222222222222222` |
| date | 2026-07-15T09:00:00.000Z |
| runner | ubuntu24, AMD EPYC 7763 64-Core Processor, 4 cores, 15.6 GiB RAM |
| toolchain | cc (Ubuntu 13.2.0-23ubuntu4) 13.2.0, cmake version 3.28.3, node v24.4.0 |
| sampling | 50 spawns, 10 warmups, best of 5 reps, /usr/bin/time (gnu) |
| source | `history/linux-x86_64/slim-v26.6.0-2.json` |

| binary | path | features on | features off | CLI subcommands |
| --- | --- | --- | --- | ---: |
| `full` | `build/tjs` | `bundledCa` `ffi` `sqlite` `tls` `wasm` `webcrypto` | — | 8/8 |
| `min` | `dist/min/tjs` | `webcrypto` | `bundledCa` `ffi` `sqlite` `tls` `wasm` | 1/8 |
| `ffi-tls` | `dist/ffi-tls/tjs` | `bundledCa` `ffi` `tls` `webcrypto` | `sqlite` `wasm` | 2/8 |
| `lite` | `dist/lite/tjs` | — | `bundledCa` `ffi` `sqlite` `tls` `wasm` `webcrypto` | 0/8 |

#### Size

| metric | `full` | `min` | `ffi-tls` | `lite` |
| --- | ---: | ---: | ---: | ---: |
| binary size, raw (bytes) | 6 450 112 | 2 050 000 | 3 280 000 | 1 880 000 |
| binary size, gzip -9 (bytes) | 3 012 345 | 1 120 000 | 1 690 000 | 1 030 000 |
| text segment (bytes) | 5 600 000 | 1 900 000 | 3 050 000 | 1 740 000 |
| data segment (bytes) | 102 400 | 17 408 | 24 576 | 16 384 |

Ratio vs `full` — bold marks a value more than 5% worse than `full`.

| metric | `min` ÷ `full` | `ffi-tls` ÷ `full` | `lite` ÷ `full` |
| --- | ---: | ---: | ---: |
| binary size, raw (bytes) | 0.32× | 0.51× | 0.29× |
| binary size, gzip -9 (bytes) | 0.37× | 0.56× | 0.34× |
| text segment (bytes) | 0.34× | 0.54× | 0.31× |
| data segment (bytes) | 0.17× | 0.24× | 0.16× |

#### Startup, memory, CPU

| metric | `full` | `min` | `ffi-tls` | `lite` |
| --- | ---: | ---: | ---: | ---: |
| startup, noop (ms) | 8.05 | 10.10 \* | 10.30 | 9.90 |
| startup, noop MAD (ms) | 0.18 | 0.90 | 0.25 | 0.24 |
| startup, +stdlib (ms) | 9.30 | 10.90 | 11.10 | 10.60 |
| startup, +stdlib MAD (ms) | 0.22 | 0.28 | 0.29 | 0.26 |
| baseline RSS (MiB) | 11.54 | 9.25 | 9.54 | 9.06 |
| peak RSS, eventloop (MiB) | 39.58 | 24.60 | 25.65 | 24.22 |
| CPU user (s) | 1.18 | 1.96 | 2.01 | 1.94 |
| CPU sys (s) | 0.09 | 0.09 | 0.10 | 0.08 |
| CPU efficiency ((user+sys)/wall) | 1.02 | 1.02 | 1.02 | 1.02 |

Ratio vs `full` — bold marks a value more than 5% worse than `full`.

| metric | `min` ÷ `full` | `ffi-tls` ÷ `full` | `lite` ÷ `full` |
| --- | ---: | ---: | ---: |
| startup, noop (ms) | **1.25×** | **1.28×** | **1.23×** |
| startup, +stdlib (ms) | **1.17×** | **1.19×** | **1.14×** |
| baseline RSS (MiB) | 0.80× | 0.83× | 0.79× |
| peak RSS, eventloop (MiB) | 0.62× | 0.65× | 0.61× |
| CPU user (s) | **1.66×** | **1.70×** | **1.64×** |
| CPU sys (s) | 1.00× | **1.11×** | 0.89× |

\* MAD exceeds 5% of the median: unstable in this run (METHODOLOGY.md rule 4).

#### Throughput

| metric | `full` | `min` | `ffi-tls` | `lite` |
| --- | ---: | ---: | ---: | ---: |
| mandelbrot (px/s) | 355 000 | 171 000 | 169 000 | 170 500 |
| array sort (elem/s) | 2 010 000 | 1 120 000 | 1 105 000 | 1 118 000 |
| string build (items/s) | 3 900 000 | 2 100 000 | 2 080 000 | 2 095 000 |
| JSON.parse (docs/s) | 172.0 | 56.0 | 55.0 | 55.8 |
| JSON.parse (MiB/s) | 121.0 | 39.0 | 38.5 | 38.9 |
| JSON.stringify (docs/s) | 101.0 | 31.0 | 30.0 | 30.8 |
| regex match (lines/s) | 1 180 000 | 740 000 | 735 000 | 739 000 |
| regex replace (corpus/s) | 40.0 | 23.5 | 23.2 | 23.4 |
| regex split (corpus/s) | 15.5 | 5.1 | 5.0 | 5.1 |
| SHA-256 (digests/s) | 1 210 | 1 190 | 1 185 | — |
| SHA-256 (MiB/s) | 302.0 | 297.0 | 296.0 | — |
| getRandomValues (calls/s) | 70 000 | 66 000 | 65 500 | — |
| timers (timers/s) | 1 810 000 | 1 120 000 | 1 100 000 | 1 118 000 |
| microtasks (tasks/s) | 2 950 000 | 1 310 000 | 1 300 000 | 1 305 000 |
| fs write (files/s) | 3 820 | 3 910 | 3 890 | 3 905 |
| fs read (files/s) | 18 300 | 18 500 | 18 400 | 18 450 |
| fs stat (stats/s) | 26 700 | 26 900 | 26 800 | 26 850 |
| fs readdir (scans/s) | 336.0 | 340.0 | 338.0 | 339.0 |

Ratio vs `full` — bold marks a value more than 5% worse than `full`.

| metric | `min` ÷ `full` | `ffi-tls` ÷ `full` | `lite` ÷ `full` |
| --- | ---: | ---: | ---: |
| mandelbrot (px/s) | **0.48×** | **0.48×** | **0.48×** |
| array sort (elem/s) | **0.56×** | **0.55×** | **0.56×** |
| string build (items/s) | **0.54×** | **0.53×** | **0.54×** |
| JSON.parse (docs/s) | **0.33×** | **0.32×** | **0.32×** |
| JSON.parse (MiB/s) | **0.32×** | **0.32×** | **0.32×** |
| JSON.stringify (docs/s) | **0.31×** | **0.30×** | **0.30×** |
| regex match (lines/s) | **0.63×** | **0.62×** | **0.63×** |
| regex replace (corpus/s) | **0.59×** | **0.58×** | **0.58×** |
| regex split (corpus/s) | **0.33×** | **0.33×** | **0.33×** |
| SHA-256 (digests/s) | 0.98× | 0.98× | — |
| SHA-256 (MiB/s) | 0.98× | 0.98× | — |
| getRandomValues (calls/s) | **0.94×** | **0.94×** | — |
| timers (timers/s) | **0.62×** | **0.61×** | **0.62×** |
| microtasks (tasks/s) | **0.44×** | **0.44×** | **0.44×** |
| fs write (files/s) | 1.02× | 1.02× | 1.02× |
| fs read (files/s) | 1.01× | 1.01× | 1.01× |
| fs stat (stats/s) | 1.01× | 1.00× | 1.01× |
| fs readdir (scans/s) | 1.01× | 1.01× | 1.01× |

Workloads skipped because the feature is compiled out (recorded as `—`, never 0): `lite`: `crypto`.

### `macos-arm64` — slim-v26.6.0-2

**Working tree was dirty — not reproducible from `commit` alone.**
| field | value |
| --- | --- |
| commit | `2222222222222222222222222222222222222222` |
| date | 2026-07-15T10:30:00.000Z |
| runner | — |
| toolchain | — |
| sampling | — |
| source | `history/macos-arm64/slim-v26.6.0-2.json` |

| binary | path | features on | features off | CLI subcommands |
| --- | --- | --- | --- | ---: |
| `full` | `build/tjs` | `bundledCa` `ffi` `sqlite` `tls` `wasm` `webcrypto` | — | — |
| `tuned-min` | `dist/tuned-min/tjs` | `webcrypto` | `bundledCa` `ffi` `sqlite` `tls` `wasm` | — |

#### Size

| metric | `full` | `tuned-min` |
| --- | ---: | ---: |
| binary size, raw (bytes) | 6 092 848 | 2 010 112 |
| binary size, gzip -9 (bytes) | 2 917 898 | 1 102 400 |
| text segment (bytes) | — | — |
| data segment (bytes) | — | — |

Ratio vs `full` — bold marks a value more than 5% worse than `full`.

| metric | `tuned-min` ÷ `full` |
| --- | ---: |
| binary size, raw (bytes) | 0.33× |
| binary size, gzip -9 (bytes) | 0.38× |
| text segment (bytes) | — |
| data segment (bytes) | — |

#### Startup, memory, CPU

| metric | `full` | `tuned-min` |
| --- | ---: | ---: |
| startup, noop (ms) | 7.92 | 9.15 |
| startup, noop MAD (ms) | 0.29 | 0.31 |
| startup, +stdlib (ms) | — | — |
| startup, +stdlib MAD (ms) | — | — |
| baseline RSS (MiB) | — | — |
| peak RSS, eventloop (MiB) | — | — |
| CPU user (s) | — | — |
| CPU sys (s) | — | — |
| CPU efficiency ((user+sys)/wall) | — | — |

Ratio vs `full` — bold marks a value more than 5% worse than `full`.

| metric | `tuned-min` ÷ `full` |
| --- | ---: |
| startup, noop (ms) | **1.16×** |
| startup, +stdlib (ms) | — |
| baseline RSS (MiB) | — |
| peak RSS, eventloop (MiB) | — |
| CPU user (s) | — |
| CPU sys (s) | — |

#### Throughput

| metric | `full` | `tuned-min` |
| --- | ---: | ---: |
| mandelbrot (px/s) | 537 552 | 470 000 |
| array sort (elem/s) | — | — |
| string build (items/s) | — | — |
| JSON.parse (docs/s) | — | — |
| JSON.parse (MiB/s) | — | — |
| JSON.stringify (docs/s) | — | — |
| regex match (lines/s) | — | — |
| regex replace (corpus/s) | — | — |
| regex split (corpus/s) | — | — |
| SHA-256 (digests/s) | — | — |
| SHA-256 (MiB/s) | 313.3 | 305.1 |
| getRandomValues (calls/s) | — | — |
| timers (timers/s) | 1 901 292 | 1 750 000 |
| microtasks (tasks/s) | — | — |
| fs write (files/s) | — | — |
| fs read (files/s) | — | — |
| fs stat (stats/s) | — | — |
| fs readdir (scans/s) | — | — |
| wasm-fib (—) | 4 200 | — |

Ratio vs `full` — bold marks a value more than 5% worse than `full`.

| metric | `tuned-min` ÷ `full` |
| --- | ---: |
| mandelbrot (px/s) | **0.87×** |
| array sort (elem/s) | — |
| string build (items/s) | — |
| JSON.parse (docs/s) | — |
| JSON.parse (MiB/s) | — |
| JSON.stringify (docs/s) | — |
| regex match (lines/s) | — |
| regex replace (corpus/s) | — |
| regex split (corpus/s) | — |
| SHA-256 (digests/s) | — |
| SHA-256 (MiB/s) | 0.97× |
| getRandomValues (calls/s) | — |
| timers (timers/s) | **0.92×** |
| microtasks (tasks/s) | — |
| fs write (files/s) | — |
| fs read (files/s) | — |
| fs stat (stats/s) | — |
| fs readdir (scans/s) | — |
| wasm-fib (—) | — |

## Trend across releases

Ratios against `full` from the *same* run, per METHODOLOGY.md rule 5: absolute
numbers from different jobs are not comparable, the ratio is. Ordered oldest first.

### `linux-x86_64`

#### `full` absolute size (bytes)

The one metric that is exact and machine-independent, so it is shown absolute
rather than as a ratio.

| version | raw | gzip -9 |
| --- | ---: | ---: |
| slim-v26.5.0-1 | 6 400 000 | 3 000 000 |
| slim-v26.6.0-2 | 6 450 112 | 3 012 345 |

#### `min` ÷ `full`

| version | size raw | size gzip | startup noop | startup +stdlib | baseline RSS | peak RSS | mandelbrot | timers | SHA-256 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| slim-v26.5.0-1 | 0.33× | 0.38× | **1.26×** | **1.17×** | 0.82× | 0.63× | **0.48×** | **0.61×** | 0.98× |
| slim-v26.6.0-2 | 0.32× | 0.37× | **1.25×** | **1.17×** | 0.80× | 0.62× | **0.48×** | **0.62×** | 0.98× |

#### `ffi-tls` ÷ `full`

| version | size raw | size gzip | startup noop | startup +stdlib | baseline RSS | peak RSS | mandelbrot | timers | SHA-256 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| slim-v26.5.0-1 | 0.52× | 0.57× | **1.28×** | **1.19×** | 0.84× | 0.66× | **0.48×** | **0.61×** | 0.98× |
| slim-v26.6.0-2 | 0.51× | 0.56× | **1.28×** | **1.19×** | 0.83× | 0.65× | **0.48×** | **0.61×** | 0.98× |

#### `lite` ÷ `full`

| version | size raw | size gzip | startup noop | startup +stdlib | baseline RSS | peak RSS | mandelbrot | timers | SHA-256 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| slim-v26.5.0-1 | — | — | — | — | — | — | — | — | — |
| slim-v26.6.0-2 | 0.29× | 0.34× | **1.23×** | **1.14×** | 0.79× | 0.61× | **0.48×** | **0.62×** | — |

### `macos-arm64`

Only one run recorded on this platform; a trend needs at least two.

## Limits

- Hosted runners are shared CPUs with noisy neighbours. Expect **5–15% run-to-run variance** on
  wall-clock metrics. **A ±10% wall-clock move is not a regression.**
- "Cold startup" means a fresh *process*, not a cold page cache. The binary is already resident
  after the warmup spawns, so these numbers are a floor, not a first-run-on-a-cold-disk figure.
- No CPU pinning, no `nice`, no isolated cpuset. `cpuEfficiency` = `(user+sys)/wall` is reported
  precisely so threaded work (mimalloc background threads, GC) is visible rather than hidden
  inside a flat wall clock.
- Peak RSS comes from `/usr/bin/time`, because txiki.js has no in-process `uv_getrusage`
  binding. If that tool is absent the driver fails loudly rather than reporting 0.
- Binary size is the one metric here that is exact and machine-independent. Trust it the most.

## Methodology

### Rules

1. **One machine, one job, serial.** All binaries are measured back-to-back in a single job on
   a single machine. Timing and RSS numbers from different jobs are never compared to each
   other. Only the `vs full` ratio is comparable across runs.
2. **Runner pinned.** CI legs pin `ubuntu-24.04` and `macos-15`, never `*-latest`, so image
   drift shows up as a version change rather than as noise.
3. **Warmup then measure.** 10 warmup spawns then 50 measured spawns for startup; best-of-5
   reps for in-process throughput. Minimum is the most stable estimator for compute-bound work,
   so `ops/sec` uses the best rep, not the mean.
4. **Report median + MAD**, never mean + stddev. Anything whose MAD exceeds 5% of its median is
   flagged as unstable *for that run* rather than silently averaged in.
5. **Normalise to `full`.** Every table carries a `vs full` ratio column. Cross-release trends
   use that ratio, because absolute numbers on hosted runners drift with image and hardware.
6. **Report-only.** There is no regression gate, exactly like the size gate in `dist.yml` before
   budgets existed. A gate on the size and startup ratios can follow once several releases of
   history exist.
7. **Missing is not zero.** A metric whose tool is unavailable (no `size(1)`) or whose feature is
   compiled out of the binary under test records `null` and renders as `—`. It never becomes 0
   and never enters an average.

### What is measured

| id | how built |
| --- | --- |
| `full` | `make` — Release, all features, full CLI. The recipe `release.yml` ships as the official asset |
| `balanced-min` | `node scripts/build-dist.mjs --profile min --optimization balanced` |
| `tuned-min` | `node scripts/build-dist.mjs --profile min --optimization tuned` |
| `min` | `node scripts/build-dist.mjs --profile min` — no FFI, no TLS |
| `ffi` | `--profile ffi` |
| `tls` | `--profile tls` |
| `ffi-tls` | `--profile ffi-tls` |

All binaries come from the **same commit**, so the feature/flag delta is the only variable.

> `scripts/build-dist.mjs` rewrites `src/bundles/`. It restores them on every exit path, but the
> driver must still build `full` **first** and assert `git diff --quiet src/bundles/c` between
> builds — a dirty bundle tree silently changes what the next binary contains.
